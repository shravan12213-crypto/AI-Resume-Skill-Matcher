# Ranking

Candidates are ranked based on their `final_score` generated by the matching algorithm. This ranking is retrieved using the `get_top_candidates` stored procedure or querying the `job_match_view`.\n