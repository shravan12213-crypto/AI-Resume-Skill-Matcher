# Database Overview

The project relies on a 3NF normalized PostgreSQL database featuring 11 core tables. The database handles the entire lifecycle of users (candidates and recruiters), job postings, resume metadata, and skill matching.

Advanced DBMS features (views, triggers, stored procedures, transactions, and indexing) are integrated deeply into the operational flow.\n