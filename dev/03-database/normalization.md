# Normalization

The database is designed up to **3NF**.

## 1NF (First Normal Form)
- Atomic values.
- No repeating groups.
- (Explanation of how our tables achieve this.)

## 2NF (Second Normal Form)
- No partial dependency on part of a composite key.
- (Explanation using `candidate_skills` or `job_skills`.)

## 3NF (Third Normal Form)
- No transitive dependencies.
- (Explanation using `users` and `candidates`/`recruiters` tables.)\n