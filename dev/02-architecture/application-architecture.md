# Application Architecture

## Frontend
- React-based Single Page Application (SPA).
- Communicates with the backend via RESTful APIs.

## Backend
- Node.js and Express server.
- Handles user requests, authentication, file uploads, and acts as the intermediary between the Frontend, the Database, and the AI Module.\n